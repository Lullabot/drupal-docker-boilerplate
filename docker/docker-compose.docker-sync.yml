# Use Docker-Sync for Mac OSX performance
# See README.md for more information.

version: "2"

services:

  php:
    volumes:
      - docker-sync:/var/www/html
  nginx:
    volumes:
      - docker-sync:/var/www/html
#  apache:
#    volumes:
#      - docker-sync:/var/www/html

volumes:
  docker-sync:
    external: true